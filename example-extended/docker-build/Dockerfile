FROM dmotte/desktainer

# Install additional packages
RUN apt-get update \
    && apt-get install -y git htop nano tmux tree wget zip curl socat procps \
        firefox-esr

#TODO sshd + siab + other packages see t.o.Dockerfile
#TODO append sshd and siab to supervisord.conf
#TODO desktop background color?
